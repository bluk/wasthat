# Return Number

A simple library to illustrate a very basic Wasm module.

It contains a single no argument function which returns a constant number.

## Build

```sh
cargo build --target wasm32-unknown-unknown --release
```
